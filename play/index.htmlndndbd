<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>Maapkan Rusdy 😔</title>
    <style>
        * { box-sizing: border-box; margin: 0; padding: 0; }
        
        body {
            font-family: 'Segoe UI', sans-serif;
            background: url('https://w0.peakpx.com/wallpaper/424/399/HD-wallpaper-view-sky-aesthetic-thumbnail.jpg') no-repeat center center fixed;
            background-size: cover;
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            overflow: hidden;
            color: white;
        }

        body::after {
            content: "";
            position: absolute;
            inset: 0;
            background: rgba(0, 0, 0, 0.4); 
            backdrop-filter: blur(10px);
            -webkit-backdrop-filter: blur(10px);
            z-index: 1;
        }

        /* Getar Card */
        @keyframes shake {
            0% { transform: translate(1px, 1px); }
            20% { transform: translate(-3px, 0px); }
            40% { transform: translate(3px, 2px); }
            100% { transform: translate(1px, 1px); }
        }
        .shaking { animation: shake 0.2s infinite; border: 2px solid #ff4d4d !important; }

        .card {
            position: relative;
            z-index: 2;
            width: 90%;
            max-width: 320px;
            background: rgba(255, 255, 255, 0.2);
            border: 1px solid rgba(255, 255, 255, 0.3);
            border-radius: 25px;
            padding: 30px 15px;
            text-align: center;
            box-shadow: 0 15px 35px rgba(0,0,0,0.3);
        }

        .avatar {
            width: 130px;
            height: 130px;
            margin: 0 auto 20px;
            border-radius: 20px;
            border: 3px solid white;
            background-image: url('https://media.giphy.com/media/v1.Y2lkPTc5MGI3NjExNHJndXIzZ3R4bmV4bmV4bmV4bmV4bmV4bmV4bmV4bmV4bmV4bmV4JmVwPXYxX2ludGVybmFsX2dpZl9ieV9pZCZjdD1n/OPU6wUKARA8AU/giphy.gif');
            background-position: center;
            background-size: cover;
        }

        .text {
            font-size: 15px;
            min-height: 50px;
            margin-bottom: 25px;
            line-height: 1.4;
            font-weight: bold;
        }

        .btn-wrapper {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 10px;
            width: 100%;
        }

        button {
            padding: 12px 5px;
            font-size: 13px;
            font-weight: bold;
            border-radius: 12px;
            border: none;
            cursor: pointer;
            touch-action: none;
        }

        #yes { background: white; color: black; z-index: 3; }
        
        #no { 
            background: #ff4d4d; 
            color: white; 
            position: relative; 
            z-index: 1000;
            /* Transisi biar geraknya halus */
            transition: all 0.2s ease-out; 
        }
    </style>
</head>
<body>

<div class="card" id="mainCard">
    <div class="avatar" id="catGif"></div>
    <div id="text" class="text"></div>
    <div class="btn-wrapper" id="btnGroup">
        <button id="yes">Iyah dimaafin</button>
        <button id="no">Enggak!</button>
    </div>
</div>

<script>
    const music = new Audio("https://raw.githubusercontent.com/rusdyXBT/pripasi/main/play/2.mp3");
    music.loop = true;
    document.body.onclick = () => { music.play().catch(() => {}); };

    const messages = ["Upps! 😢 Harus dimaafin yaa 👀🤝", "Minal 'Aidin Walfaizin ✨", "Maapkan Rusdy yaa? 👉👈"];
    let step = 0;
    const textEl = document.getElementById("text");
    const catGifEl = document.getElementById("catGif");
    const cardEl = document.getElementById("mainCard");
    const noBtn = document.getElementById("no");

    textEl.innerHTML = messages[step];

    document.getElementById("yes").onclick = () => {
        cardEl.classList.remove("shaking");
        step++;
        if (step < messages.length) {
            textEl.innerHTML = messages[step];
            catGifEl.style.backgroundImage = "url('https://media.giphy.com/media/v1.Y2lkPTc5MGI3NjExNHJndXIzZ3R4bmV4bmV4bmV4bmV4bmV4bmV4bmV4bmV4bmV4bmV4JmVwPXYxX2ludGVybmFsX2dpZl9ieV9pZCZjdD1n/OPU6wUKARA8AU/giphy.gif')";
        } else {
            catGifEl.style.backgroundImage = "url('https://media.giphy.com/media/ICOgUNoMPvG24/giphy.gif')";
            textEl.innerHTML = "Makasih! ❤️ Sayang kamu!";
            document.getElementById("btnGroup").style.display = "none";
        }
    };

    function lari(e) {
        if(e) e.preventDefault();
        
        // GIF Cony Brown Marah
        catGifEl.style.backgroundImage = "url('https://media1.tenor.com/m/Xo7F7x7uD7kAAAAC/cony-and-brown-angry.gif')";
        cardEl.classList.add("shaking");
        textEl.innerHTML = "KOK DITEKAN TERUS?! 😡";

        noBtn.style.position = "fixed";
        noBtn.style.width = "100px";

        /* --- SISTEM ANTI HILANG --- */
        // Kita batasi area random hanya di tengah layar (sekitar Card)
        const lebarTombol = 100;
        const tinggiTombol = 45;
        
        // Batas atas (Jangan terlalu atas sampai kena status bar)
        // Batas bawah (Jangan terlalu bawah sampai hilang dari layar)
        const areaX_min = 30; 
        const areaX_max = window.innerWidth - lebarTombol - 30;
        const areaY_min = 100;
        const areaY_max = window.innerHeight - tinggiTombol - 150; // Jarak 150px dari bawah layar

        const randomX = Math.floor(Math.random() * (areaX_max - areaX_min)) + areaX_min;
        const randomY = Math.floor(Math.random() * (areaY_max - areaY_min)) + areaY_min;

        noBtn.style.left = randomX + "px";
        noBtn.style.top = randomY + "px";
    }

    noBtn.addEventListener("touchstart", lari);
    noBtn.addEventListener("mouseover", lari);
</script>
</body>
</html>
